name: naina_example
description: Minimal example — pick an image, read its text on-device.
publish_to: 'none'
version: 0.2.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  naina:
    path: ../
  image_picker: ^1.1.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
