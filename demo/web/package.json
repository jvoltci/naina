{
  "name": "naina-web-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Live in-browser face recognition demo for naina. Detects N faces, lets users enrol, recognises across frames. Runs the same model artifacts naina's native lib will use.",
  "scripts": {
    "fetch-models": "bash scripts/fetch-models.sh",
    "predev": "npm run fetch-models",
    "prebuild": "npm run fetch-models",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "onnxruntime-web": "^1.18.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
