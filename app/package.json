{
  "name": "naina-web-demo",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "In-browser OCR demo for naina. Reads a document image entirely client-side using naina's C++ core compiled to WebAssembly.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@jvoltci/naina-wasm": "file:../bindings/wasm",
    "onnxruntime-web": "^1.27.0",
    "pdfjs-dist": "^6.2.108"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
